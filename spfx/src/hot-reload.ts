/**
 * You can import this file at the top of your webpart or extension.
 * Doing so will enable hot-reloading when something in /component changes while serveing.
 * This works because the serve task of /component writes on this file after re-bundling.
 */
